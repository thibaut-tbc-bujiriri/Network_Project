/** @type {import('tailwindcss').Config} */
export default {
  // Configuration minimale pour Tailwind CSS v4.
  // Le thème (couleurs primary, etc.) est désormais défini dans le CSS via `@theme`.
}

